# L5: requisitos mínimos só para a dashboard / validation platform.
# Não inclui SUMO/traci (~150 MB) — útil para utilizadores que apenas
# precisam de visualizar artefactos já gerados.
#
# Uso: pip install -r requirements-dashboard.txt
#      (em alternativa a requirements.txt para evitar instalar SUMO)
streamlit>=1.36,<2.0
pandas>=2.0,<3.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
defusedxml>=0.7,<1.0
# PyYAML usado pela dashboard para carregar configs/scenarios.yaml.
# Vem normalmente como dep transitiva do streamlit, mas declaramos
# explicitamente para não depender de um detalhe da árvore de pacotes.
PyYAML>=6.0,<7.0
