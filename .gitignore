# Local Python environment
.venv/

# Python caches and packaging
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/
*.egg-info/

# Local environment files
.env
.env.*

# macOS/editor files
.DS_Store
.idea/
.vscode/

# Generated SUMO/pipeline outputs.
# Padrões recursivos (**/): cobrem também subdiretórios de snapshot como
# outputs/runs/baseline/ e outputs/runs/rl/ produzidos por compare-tsp-rl.
outputs/**/*.xml
outputs/**/*.csv
outputs/**/*.json
outputs/**/*.jsonl
outputs/**/*.log
outputs/**/*.sumocfg
reports/**/*.json
reports/**/*.md
sumo/network/*.net.xml
sumo/outputs/

# Editor / tooling metadata (downloads helpers, gh cache, etc.)
.tools/
# Ignore local Claude Code config (settings.local.json, locks) but track shared skills.
.claude/*
!.claude/skills/

# Source patch artifacts already applied to the repository.
*.patch
*.patch.bak
